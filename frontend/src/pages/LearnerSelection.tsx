import { useState } from "react"; import type { Learner } from "../types";
export function LearnerSelection({ learners, onSelect, onCreate }: { learners: Learner[]; onSelect: (learner: Learner) => void; onCreate: (name: string) => void }) { const [name, setName] = useState(""); return <section><h2>Learners</h2><form onSubmit={event => { event.preventDefault(); if (name) onCreate(name); }} className="flex gap-2"><input aria-label="Learner name" value={name} onChange={e => setName(e.target.value)} className="border p-2" placeholder="New learner"/><button className="rounded bg-indigo-600 px-3 py-2 text-white">Create</button></form><div className="mt-4 grid gap-2">{learners.map(learner => <button key={learner.id} onClick={() => onSelect(learner)} className="rounded border p-3 text-left">{learner.name} · Grade {learner.grade}</button>)}</div></section>; }

import { useState } from "react"; import type { Question } from "../types";
export function Quiz({ question, onSubmit }: { question?: Question; onSubmit: (answer: string, hints: number) => void }) { const [answer, setAnswer] = useState(""); const [hints, setHints] = useState(0); if (!question) return <p>Choose a learner to load a question.</p>; return <section><h2>Quick check</h2><p className="my-4 text-lg">{question.text}</p><div className="grid gap-2">{question.options.map(option => <label key={option} className="rounded border p-2"><input type="radio" name="answer" value={option} onChange={() => setAnswer(option)}/> {option}</label>)}</div><div className="mt-4 flex gap-2"><button onClick={() => setHints(hints + 1)} className="border px-3 py-2">Hint ({hints})</button><button disabled={!answer} onClick={() => onSubmit(answer, hints)} className="bg-indigo-600 px-3 py-2 text-white disabled:opacity-50">Submit</button></div></section>; }
